

export const FILTER_OPTION = Object.freeze({
    NONE: "None",
    SEPIA: "Sepia",
    GRAYSCALE: "Grayscale",
    INVERT: "Invert",
    OPACITY: "Lighten"
});

export const FORM_OPTION = Object.freeze({
    SELECT: "SELECT",
    INPUT: "INPUT"
});
